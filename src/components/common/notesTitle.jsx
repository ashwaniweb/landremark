import React from "react";
// import { FormGroup, FormControl } from "react-bootstrap";
export const NotesTitle = props => {
  return (
    <div className="page-header mt-0">
      <h3>{props.title}</h3>
    </div>
  );
};
